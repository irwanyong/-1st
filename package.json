{
  "name": "1st",
  "scripts": {
    "fmt": "biome check --write",
    "bootstrap": "bun install",
    "clean": "rm -rf node_modules && bun run --workspaces clean"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "database"
  ],
  "devDependencies": {
    "@biomejs/biome": "2.2.6"
  }
}
